package com.palco.api.repository;

import com.palco.api.model.Cifra;
import org.springframework.data.jpa.repository.JpaRepository;

public interface CifraRepository extends JpaRepository<Cifra, Long> {
}
